---
# Staging ResourceQuota — lower limits than production since staging
# shares a single VPS node with the production namespace.
apiVersion: v1
kind: ResourceQuota
metadata:
  name: mindledger-staging-quota
  namespace: mindledger-staging
  labels:
    app.kubernetes.io/name: mindledger
    app.kubernetes.io/component: resource-quota
    environment: staging
spec:
  hard:
    requests.cpu: "2"
    limits.cpu: "4"
    requests.memory: 2Gi
    limits.memory: 4Gi
    pods: "15"
    persistentvolumeclaims: "8"
    services: "10"
    secrets: "10"
    configmaps: "10"
---
apiVersion: v1
kind: LimitRange
metadata:
  name: mindledger-staging-limit-range
  namespace: mindledger-staging
  labels:
    app.kubernetes.io/name: mindledger
    app.kubernetes.io/component: resource-quota
    environment: staging
spec:
  limits:
    - type: Container
      default:
        cpu: "250m"
        memory: "256Mi"
      defaultRequest:
        cpu: "50m"
        memory: "64Mi"
      max:
        cpu: "1"
        memory: "1Gi"
      min:
        cpu: "25m"
        memory: "32Mi"
