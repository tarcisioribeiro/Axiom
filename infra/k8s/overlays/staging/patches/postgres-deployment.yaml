apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: axiom
spec:
  template:
    spec:
      containers:
        - name: postgres
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
