apiVersion: v1
kind: ResourceQuota
metadata:
  name: axiom-quota
  namespace: axiom
spec:
  hard:
    requests.cpu: "2"
    limits.cpu: "4"
    requests.memory: 4Gi
    limits.memory: 6Gi
    pods: "15"
    persistentvolumeclaims: "8"
    services: "10"
    secrets: "10"
    configmaps: "10"
