# Development dependencies - not needed in production
# Install with: pip install -r requirements-dev.txt
# NOTE: first run `pip install -e api/` from the repo root to register local flake8 plugins.

# Include production dependencies
-r requirements.txt

# Testing dependencies
pytest==9.1.1
pytest-django==4.12.0
pytest-cov==7.1.0
freezegun==1.5.5

# Code quality dependencies (kept in sync with requirements-lint.txt, used
# standalone by CI's lint:backend/lint:bandit/lint:pip-audit jobs)
-r requirements-lint.txt
pre-commit==4.6.1

# Type checking
mypy==2.3.0
django-stubs==6.0.7
djangorestframework-stubs==3.17.1
